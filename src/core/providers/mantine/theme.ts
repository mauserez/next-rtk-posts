import { createTheme, DEFAULT_THEME } from "@mantine/core";
export const theme = createTheme({
	autoContrast: true,
	radius: {
		xs: "7px",
		sm: "11px",
		md: "13px",
		lg: "16px",
		xl: "19px",
	},
});
